# Todo Next

## Active Target

- POST-PHASE — Hardening, live integration refinement, and operational stabilization

## Current Next Step

- Continue acting on the external audit triage after the runtime Risk/OMS gate, durable recovery, broker-source MT5 hedging, deal-based accounting, protective SL/TP, position-protection fail-safe, MT5 symbol enforcement, reconnect/fault-injection coverage, Docker/Compose paper validation, full Ruff/mypy cleanup, CI/local gate promotion, production CLI slices, risk-budget guards, bid/ask backtest slice, risk-budget config/runtime broker-context wiring, supervised baseline and transformer training/runtime package export, first FX realism metrics, margin-call liquidation, stop/TP path simulation, broker symbol-spec ingestion, historical data bootstrap slices, EURUSD Dukascopy raw/offline processing pass, and first verified EURUSD 2024 M1 training/backtest pilot: next prepare a reproducible VPS/GPU training workflow for RTX 4070 16 GB, scale the training/backtest workflow beyond the 2024 M1 smoke subset, add direct broker/MT5 history download and versioned provenance, deepen FX execution-path calibration, add model registry/drift monitoring, extend supervision into longer paper/shadow evidence with real alert sink topology, and continue production-startup hardening.
- Keep the completed Parallels MT5 demo-order findings in scope while doing this work: Dukascopy `margin_mode=2` behaved as hedging, IOC is required for EURUSD, and historical orders/deals are visible when an explicit terminal path plus sufficient lookback is used.
- If further MT5 work is paused, continue with runtime supervision evidence and production-startup hardening from `docs/post-phase-roadmap.md`.

## Deliverables

- Sprint A+ operational foundation from `docs/post-phase-roadmap.md` — completed on 2026-04-27
- MT5 safe submit chain with `order_check -> order_send` guardrails — completed on 2026-04-27
- unified event journal contracts for market/signal/order/fill/risk/latency events — completed on 2026-04-27
- standalone OMS/RiskEngine state machine with kill switches and emergency flatten workflow — completed on 2026-04-27
- full Python 3.11+ environment validation, including repo-wide test execution in the actual target interpreter — completed on 2026-04-27 with Python 3.12.13
- execution-aware simulator V2 with latency, partial-fill, cancel/replace, stale/closed-market, queue-position, and execution-quality metrics — completed on 2026-04-27
- baseline strategy suite with spread/mean-reversion, OFI/imbalance, volatility breakout, sensitivity scenarios, and walk-forward reports — completed on 2026-04-28
- unified validation gate report for backtest, walk-forward, execution stress, latency/slippage, risk flags, and regime breakdown — completed on 2026-04-28
- paper/shadow champion-challenger decision reporting — completed on 2026-04-28
- interpretable supervised baseline filter with leakage-safe walk-forward evaluation — completed on 2026-04-28
- observability alert-rule docs and platform roadmap — completed on 2026-04-28
- production checklist, incident/postmortem templates, and release/incident runbooks — completed on 2026-04-28
- Windows MT5 terminal connection and broker-side order_check smoke against an authorized demo session — completed on 2026-04-28 without order_send
- safe MT5 broker probe through the normalized client, including terminal/account/symbol/tick/history diagnostics and broker-side FOK order_check — completed on 2026-04-28 without order_send
- MT5 Python bridge order-comment limit discovery and client-side sanitize/clamp fix — completed on 2026-04-28
- paper-safe Docker/Compose runtime packaging around the PHASE 12 runtime — completed on 2026-04-28 as source/config; Docker Desktop build/run validation completed on 2026-05-03
- full-repo Ruff/mypy cleanup baseline — completed on 2026-04-28 as `docs/lint-typecheck-baseline.md`; cleanup completed on 2026-05-03 and the checks are now part of the local/CI gate
- local JSONL alert transport for health snapshots — completed on 2026-04-28; network alert transport remains pending after runtime topology validation
- first scripts Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `511` to `496`
- config-layer Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `496` to `434`
- HTTP webhook alert transport — completed on 2026-04-28 with config/env wiring and connected to `run_runtime.py supervise` on 2026-05-01; concrete endpoint values remain pending until deployment topology is chosen
- logging-utils Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `434` to `433`
- journal Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `433` to `416`
- OMS Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `416` to `409`
- validation Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `409` to `402`
- models Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `402` to `392`
- risk Ruff cleanup batch — completed on 2026-04-28; full Ruff backlog reduced from `392` to `374`
- data-layer Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `374` to `163`
- backtesting Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `163` to `138`
- features Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `138` to `93`
- RL Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `93` to `77`
- deployment Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `77` to `72`
- execution Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `72` to `50`
- domain and final UTC Ruff cleanup batch — completed on 2026-05-03; full Ruff backlog reduced from `50` to `0`
- Parallels Windows 11 MT5 environment and Dukascopy demo validation — completed on 2026-04-28 without SSH and without `order_send`: `mt5_smoke.py` connected to account `610769553`; EURUSD IOC `order_check` passed with `retcode=0`; EURUSD FOK was rejected as unsupported filling mode
- Parallels deeper MT5 history/deal check — initial 2026-04-28 read-only probe returned no rows before the controlled fill; 2026-04-30 follow-up with explicit terminal path and 8760-hour lookback resolved visibility with 4 EURUSD historical orders / 4 trade deals plus a zero-volume deposit deal
- Docker/runtime validation — completed on 2026-05-03: Docker Desktop Engine `29.4.1` was reachable, `docker build -t forex-scalper-ai:local .` passed, Compose `paper-runtime describe` returned paper mode, Compose `health` returned `overall_status=pass`, Compose `metrics` emitted Prometheus-style runtime metrics, and the test Compose stack was cleaned up
- Controlled Parallels MT5 demo-order validation — completed on 2026-04-29 after explicit operator approval: minimum-volume EURUSD IOC order filled, initial auto-flatten exposed hedging behavior, position-ticket flatten closed all remaining EURUSD positions, final smoke showed zero open orders/positions, and later explicit-terminal-path / 8760-hour probing confirmed historical order/deal visibility
- External live-readiness audit triage — completed on 2026-04-30 as `docs/external-audit-2026-04-30.md`; confirmed P0 gaps are mandatory Risk/OMS runtime wiring, durable state/recovery, broker-source-of-truth MT5 sizing, hedging-aware execution/reconciliation, deal-based accounting, and protective order management
- P0.A first runtime gate slice — completed on 2026-04-30: `DeploymentRuntime.submit_order()` now enforces RiskEngine before router/broker submit, drives OMS checked/sent/final/process-update or risk-rejected states, and records risk/OMS/execution journal events; targeted Ruff passed and full `.venv/bin/pytest` passed with `173 passed`
- P0.B first durable recovery slice — completed on 2026-04-30: `SqliteExecutionStateStore` persists runtime order/risk/OMS/execution/fill/position/kill-switch state, `DeploymentRuntime.start()` reloads state before new orders, duplicate intents after restart are blocked by recovered state, open live recovered orders block unsafe paper fallback or unreconciled live startup, and full `.venv/bin/pytest` passed with `176 passed`
- P0.C first broker-source MT5 hedging slice — completed on 2026-04-30: MT5 sizing refreshes broker positions before target/reduce-only decisions, hedging mode tracks tickets and gross exposure, single-ticket reduce-only closes pass MT5 `position`, ambiguous multi-ticket closes are rejected, reconciliation flags hidden hedged gross exposure, the MT5 overlay defaults to hedging for Dukascopy demo behavior, targeted Ruff passed, targeted pytest passed with `33 passed`, and full `.venv/bin/pytest` passed with `182 passed`
- P0.D first deal-based live accounting slice — completed on 2026-04-30: MT5 deals are normalized as `Mt5DealState`, `Mt5OrderState` carries deal records, the MT5 live adapter builds fills from unseen broker deal ids with commission/fee/swap costs before falling back to cumulative deltas, script JSON serialization includes normalized deal records, targeted Ruff passed, targeted pytest passed with `22 passed`, and full `.venv/bin/pytest` passed with `183 passed`
- P0.E first protective-order slice — completed on 2026-04-30: `OrderIntent` carries optional `stop_loss_price`/`take_profit_price`, MT5 requests/states map them to native `sl`/`tp`, MT5 config can require SL and/or TP for exposure-increasing orders, reconciliation flags missing/mismatched broker protective prices after acknowledgement, `mt5_smoke.py` serializes protective fields, targeted Ruff passed, targeted pytest passed with `43 passed`, compileall passed, and full `.venv/bin/pytest` passed with `188 passed`
- P0.E position-protection fail-safe slice — completed on 2026-04-30: broker position snapshots now carry SL/TP protection, MT5 position normalization maps `sl`/`tp`, reconciliation flags live MT5 positions missing required protection after fill/reconnect, and `DeploymentRuntime` activates a durable session kill-switch when that drift appears; targeted Ruff passed and targeted pytest passed with `51 passed`
- P1 risk-config guard slice — completed on 2026-04-30: `RiskEngine` now enforces max spread, post-loss cooldown, volatility/news guards, feature/model health, and recovered durable kill-switch state before router/broker submission; live runtime injects spread into risk context; targeted Ruff passed and targeted pytest passed with `40 passed`
- P1 MT5 symbol-spec quantization slice — completed on 2026-04-30: `Mt5TerminalClient.get_symbol_spec()` normalizes broker `symbol_info`, `Mt5ExecutionAdapter` uses symbol specs when available, lot sizing and broker lot-to-base-unit normalization now use symbol contract size plus `Decimal` `ROUND_DOWN` by broker `volume_min`/`volume_step`/`volume_max`, oversize/undersize quantities return structured rejections, targeted Ruff passed, targeted pytest passed with `23 passed`, combined safety/MT5 pytest passed with `61 passed`, compileall passed, and full `.venv/bin/pytest` passed with `199 passed`
- MT5 symbol metadata enforcement slice — completed on 2026-04-30: `Mt5SymbolSpec` carries broker trade/filling/execution modes, `Mt5ExecutionAdapter` quantizes request prices by broker `point`/`digits`, rejects entry/protective prices inside stops level, enforces trade-mode exposure restrictions, selects/rejects FOK/IOC from symbol `filling_mode`, and rejects FOK/IOC on pending orders; targeted Ruff passed, targeted pytest passed with `32 passed`, broader runtime/MT5/config/preflight pytest passed with `67 passed`, compileall passed, and full `.venv/bin/pytest` passed with `220 passed`
- P1 MT5 reconnect supervision slice — completed on 2026-04-30: `Mt5TerminalClient` now probes terminal/account readiness before use, reconnects stale sessions, exposes a connection snapshot, opens a circuit breaker after configured failed reconnect attempts, and wires reconnect config/env/live-factory settings; targeted Ruff passed, targeted pytest passed with `23 passed`, compileall passed, and full `.venv/bin/pytest` passed with `205 passed`
- Bracket/OCO lifecycle and approved flatten slice — completed on 2026-04-30: reconciliation now infers expected position SL/TP protection from filled bracket intents, flags missing/mismatched/ambiguous broker-side position protection after fills or reconnects, and `DeploymentRuntime.flatten_unprotected_positions()` submits explicit-token reduce-only flatten orders for drifted live positions; targeted Ruff passed, targeted pytest passed with `35 passed`, expanded runtime/MT5/bootstrap pytest passed with `47 passed`, compileall passed, and full `.venv/bin/pytest` passed with `210 passed`
- Durable per-deal attribution slice — completed on 2026-04-30: `FillEvent` carries broker deal attribution fields, MT5 deal fills populate broker deal/position/cost fields, SQLite execution state schema v2 persists `deal_attributions`, and FILL journal payloads preserve raw broker commission/fee/swap alongside normalized execution cost; targeted Ruff passed, targeted pytest passed with `26 passed`, broader execution/runtime pytest passed with `40 passed`, compileall passed, and full `.venv/bin/pytest` passed with `211 passed`
- Broker-side startup recovery/fault-injection slice — completed on 2026-04-30: live startup now requires a reconciliation report after successful live adapter activation, blocks missing/failed startup reconciliation, activates a durable session kill-switch for error-level drift, and tests broker-only exposure before the first health poll plus provider exception/empty-report faults; targeted Ruff passed, targeted pytest passed with `38 passed`, broader runtime/MT5/bootstrap pytest passed with `51 passed`, compileall passed, and full `.venv/bin/pytest` passed with `214 passed`
- MT5 protective repair/modify first slice — completed on 2026-04-30: added dedicated `Mt5ProtectionUpdateRequest`, `Mt5TerminalClient.modify_position_protection()` with `order_check -> order_send` using `TRADE_ACTION_SLTP`, adapter-side stops/freeze/trade-mode validation before repair, and `DeploymentRuntime.repair_unprotected_positions()` for explicit-token SL/TP repair from reconciliation issues with unambiguous expected prices; targeted Ruff passed, targeted MT5/client/runtime pytest passed with `60 passed`, broader MT5/runtime/config/preflight pytest passed with `85 passed`, compileall passed, and full `.venv/bin/pytest` passed with `225 passed`
- Post-repair reset / pending-order modify / history-probe / supervisor slice — completed on 2026-04-30: runtime can clear a session kill-switch only after explicit-token clean reconciliation, MT5 supports pending-order `TRADE_ACTION_MODIFY` through `order_check -> order_send` with stops/freeze guardrails, `scripts/mt5_history_probe.py` investigates raw history/deal API call shapes without `order_send`, `RuntimeSupervisor` schedules health/metrics/reconciliation polling, targeted Ruff passed, targeted pytest passed with `71 passed`, compileall passed, and full `.venv/bin/pytest` passed with `236 passed`
- Real Parallels read-only MT5 history/deal validation — completed on 2026-04-30: `mt5_history_probe.py` with explicit `BROKER_MT5_TERMINAL_PATH` and `8760` hour lookback found 4 EURUSD historical orders and 4 EURUSD trade deals without `order_send`; `Mt5TerminalClient` now filters non-trade/zero-volume deposit deals before per-order normalization; `mt5_broker_probe.py` returned `normalized_order_count=4` with deal attribution and full `.venv/bin/pytest` remained at `236 passed`
- Supervisor alert routing and controlled pending-order modify demo — completed on 2026-05-01: `RuntimeSupervisor` now sends alert events through injected transports, `scripts/run_runtime.py supervise` wires optional JSONL/webhook alert routing, `scripts/mt5_pending_modify_demo.py` placed/modified/canceled a far-away EURUSD pending demo order on Dukascopy account `610769553`, final smoke showed zero open orders/positions, and MT5 history ticket filtering was hardened after the live cancel path exposed unrelated history rows
- Richer runtime health provider contracts — completed on 2026-05-01: added typed data freshness, model readiness, and volatility/news guard snapshots/providers; `DeploymentRuntime` now exposes `data_freshness`, `model_readiness`, `dependency_guards`, and `risk_guardrails` checks plus metrics, routes provider state into pre-trade `RiskContext`, warns on missing live providers, fails live health on stale data/unready model, surfaces broker reconnect attempt/circuit-breaker state, and passed full `.venv/bin/python -m pytest` with `250 passed`
- Concrete runtime dependency provider trackers — completed on 2026-05-03: `RuntimeDataFreshnessProvider`, `RuntimeModelHealthProvider`, and `RuntimeGuardStateProvider` can now be updated by live/paper loops and exposed directly through the runtime health provider contracts; targeted Ruff, compileall, `git diff --check`, targeted provider/runtime pytest, and full `.venv/bin/python -m pytest` passed with `254 passed`
- Concrete runtime dependency provider event-loop wiring — completed on 2026-05-03: runtime `submit_order()` and `process_quote()` now update concrete data freshness from execution quotes, optional `OnlineFeatureCalculator` output feeds data/guard providers, runtime exposes direct market/feature/model provider hooks, `bootstrap_runtime()` accepts the provider/calculator bundle, targeted provider/runtime/bootstrap pytest passed with `45 passed`, paper runtime describe/health passed, and full `.venv/bin/python -m pytest` passed with `258 passed`
- MT5 partial-fill fault-injection validation — completed on 2026-05-03: `Mt5TerminalClient` fallback normalization preserves `TRADE_RETCODE_DONE_PARTIAL` result volume when broker history/order refresh is temporarily unavailable, clamps impossible oversized partial result volume, and `Mt5ExecutionAdapter` tests cover incremental partial broker deal fills across successive `process_quote()` polls without duplicate fill creation; targeted MT5 client/live pytest passed with `42 passed` and full `.venv/bin/python -m pytest` passed with `261 passed`
- MT5 post-send fallback fault-injection validation — completed on 2026-05-03: `Mt5TerminalClient` now returns normalized success fallback state when a successful `order_send` cannot be immediately refreshed from order/history/deal APIs or lacks a numeric ticket, while explicit non-success send retcodes remain structured rejections; targeted MT5 client pytest passed with `25 passed` and full `.venv/bin/python -m pytest` passed with `265 passed`
- Docker/Compose paper-runtime validation — completed on 2026-05-03: Docker Desktop build passed, Compose `describe`/`health`/`metrics` passed in the paper profile, health returned `overall_status=pass`, and the Compose stack was cleaned up
- Docker/Compose bounded supervisor validation — completed on 2026-05-03: `compose-supervise` / Compose `paper-runtime supervise` completed 5 safe paper iterations with health/reconciliation due, `overall_status=pass`, rendered metrics, and zero alerts/errors
- Longer local paper supervisor evidence — completed on 2026-05-03: `make supervise-paper` was added and a 30-iteration paper run passed with `30 pass`, health/reconciliation due on every iteration, rendered metrics, zero runtime errors, zero alert transport errors, and zero alerts; `docs/runtime-supervision-evidence.md` records the evidence and a read-only Parallels MT5 smoke
- Wall-clock local paper supervisor evidence — completed on 2026-05-03: `RuntimeSupervisor` and `run_runtime.py supervise` now support `max_runtime_seconds`, bounded supervisor JSON artifacts can be persisted with `--output-path`, `make supervise-paper-wallclock` and `compose-supervise-wallclock` wrap the flow, a 2-second local run produced 11 `pass` iterations with zero errors or alerts, and `make PYTHON=.venv/bin/python ci` passed with full pytest `268 passed`
- Fresh-checkout Parallels MT5 validation — completed on 2026-05-03: cloned `forex-scalper-ai-current` inside Windows 11, verified current `main` commit `86afc80`, ran read-only smoke and safe EURUSD IOC broker probe with explicit terminal path, confirmed zero open orders/positions, `order_check.accepted=true` with `retcode=0` / `Done`, raw 8760-hour history visibility, and `order_send_called=false`
- External audit 2026-05-03 live-safety triage — completed on 2026-05-03: `docs/external-audit-2026-05-03.md` records the new report review; confirmed fixes were applied for the unsupported `configs/live.yaml` adapter, live startup missing dependency providers, and UTC day-start equity tracking for daily drawdown risk; `make PYTHON=.venv/bin/python ci` passed with full pytest `270 passed`
- External audit 2026-05-03 parallel follow-ups — completed on 2026-05-03: added clean source archive tooling, offline data-quality validation reports for tick/replay data, and production model bundle metadata contracts; targeted Ruff/compile/tests passed with `20 passed`, and `make PYTHON=.venv/bin/python ci` passed with full pytest `290 passed`
- Production CLI first slice — completed on 2026-05-03: added `scripts/build_dataset.py`, `scripts/run_backtest.py`, `scripts/run_walk_forward.py`, shared `scripts/cli_utils.py`, unit coverage, and `docs/production-cli.md` for UTC-safe dataset building, explicit-cost baseline backtests, and baseline walk-forward validation; `make PYTHON=.venv/bin/python ci` passed with full pytest `293 passed`
- Parallel production hardening batch — completed on 2026-05-03: added offline feature-build CLI, supervised filter walk-forward CLI, opt-in RiskEngine guards for risk-per-trade/open-position/weekly-loss/margin/leverage, and optional bid/ask-aware backtest execution while preserving mid-price defaults; `make PYTHON=.venv/bin/python ci` passed with full pytest `306 passed`
- Risk-budget config/runtime broker-context wiring — completed on 2026-05-03: added config/env fields for weekly-loss, risk-per-trade, max-open-position, margin-level, and leverage budgets; added generic broker account snapshots; wired runtime risk context to live broker account state, broker-source live positions, quote-based market entry estimates, and UTC day/week realized-PnL baselines; MT5 account snapshots now include margin fields and the MT5 adapter exposes gross-position effective leverage; `make PYTHON=.venv/bin/python ci` passed with full pytest `309 passed`
- Supervised baseline training/export and runtime inference packaging — completed on 2026-05-03: added `scripts/train_supervised_filter.py`, JSON model/scaler artifacts, SHA-verified model bundle loading, `load_baseline_filter_inference_package()`, and tests for training cutoff leakage controls and runtime prediction loading; `make PYTHON=.venv/bin/python ci` passed with full pytest `317 passed`
- Transformer training/export and runtime inference packaging — completed on 2026-05-03: added `scripts/train_transformer.py`, `model.pt`/`scaler.json`/`metadata.json` bundle export, tail validation metrics inside the selected training window, SHA-verified `load_transformer_inference_package()`, runtime scoring, schema/scaler/model-dimension checks, docs, focused tests with `8 passed`, and `make PYTHON=.venv/bin/python ci` with full pytest `334 passed`
- Historical data bootstrap CLI — completed on 2026-05-04: added `scripts/bootstrap_history.py` for broker/vendor CSV or Parquet exports, UTC timestamp normalization, explicit bid/ask or synthetic-spread assumptions, QA report gating, provenance summary JSON, and focused tests; `make PYTHON=.venv/bin/python ci` passed with full pytest `337 passed`; direct MT5 historical market-data download remains the next data-provenance slice
- Direct Dukascopy historical data downloader and first full EURUSD cache/process pass — completed on 2026-05-05: added `scripts/download_dukascopy_ticks.py` for public hourly `.bi5` download, UTC tick decoding, QA-gated bootstrap reuse, standard M1-D1 local bar derivation, failed-hour evidence, resumable range execution, coverage-first / repair-only / `--download-archives-only` / `--offline-cache-only` modes for long ranges, and focused tests; raw archive repair completed for `2016-01-01` through `2026-05-04` with `83734` hourly `.bi5` files; offline-cache processing completed with `2452` completed dates, `185162841` tick rows, `3233` daily TICK parquet files, and `3233` files for every derived M1-D1 timeframe; consolidated evidence, failed-day evidence, and session coverage evidence were written under ignored `data/artifacts/dukascopy/EURUSD/`; failed/no-data dates classified as expected market-closed/holiday dates with `0` unexpected failed session days and session coverage ratio `1.0`
- First verified EURUSD 2024 M1 training/backtest pilot — completed on 2026-05-05: generated a no-lookahead M1 tick-like input with `366847` rows, offline features with same-row bid/ask/mid price, a supervised dataset with history `32`, horizon `5`, stride `5`, `73363` rows, and `608` features; ran explicit-cost baseline and FX micro-position backtests; ran supervised filter walk-forward with `3` folds, mean accuracy `0.4993693383786263`, and coverage `1.0`; exported a runtime-loadable supervised filter bundle trained on `61516` rows through `2024-10-31T23:57:00Z`; exported a compact runtime-loadable transformer smoke bundle with validation directional accuracy `0.4972994598919784`; verified both runtime loaders on 5-row samples; fixed and tested `scripts/run_backtest.py --help` percent-format regression; targeted Ruff, compileall, `git diff --check`, and focused pytest passed with `16 passed`
- VPS/GPU training workflow — planned next on 2026-05-07: use a VPS with RTX 4070 16 GB VRAM for heavy multi-year Transformer/DRL research training, keep live/MT5 credentials off that machine, either redownload/process Dukascopy data there or transfer selected `data/processed` parquet subsets, export model bundles back to the Mac workspace, then validate through runtime loaders, backtests, walk-forward reports, and promotion gates before paper/shadow deployment
- First FX backtest realism slice — completed on 2026-05-03: added row-level execution cost columns, `FxSymbolSpec`, pip-value metrics, margin-required/utilization metrics, and rollover swap-cost accounting to the historical backtest while preserving default behavior; `make PYTHON=.venv/bin/python ci` passed with full pytest `321 passed`
- Second FX backtest realism slice — completed on 2026-05-03: added opt-in `margin_call_level`, broker-style same-row/next-row forced liquidation, margin-level/effective-leverage/liquidation metrics, CLI wiring via `--margin-call-level`, and focused unit coverage; `make PYTHON=.venv/bin/python ci` passed with full pytest `323 passed`
- Third FX backtest realism slice — completed on 2026-05-03: added opt-in high/low completed-bar stop-loss and take-profit path simulation, conservative same-bar ambiguity priority, active protection state/counters in equity rows and metrics, CLI wiring for protective path flags, and focused unit coverage; `make PYTHON=.venv/bin/python ci` passed with full pytest `326 passed`
- Fourth FX backtest realism slice — completed on 2026-05-03: added strict `FxSymbolSpec` JSON loading for broker/curated symbol assumptions, exported the loader, wired `scripts/run_backtest.py --fx-symbol-spec-path` as an explicit alternative to manual FX flags, documented the schema, added focused loader/CLI tests, and `make PYTHON=.venv/bin/python ci` passed with full pytest `329 passed`
- Data-layer Ruff cleanup — completed on 2026-05-03: `src/scalper_ai/data` and selected data tests now pass targeted Ruff; selected data pytest passed with `20 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`; full Ruff backlog is now `163`
- Backtesting Ruff cleanup — completed on 2026-05-03: `src/scalper_ai/backtesting` and selected backtesting tests now pass targeted Ruff; selected backtesting pytest passed with `14 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`; full Ruff backlog is now `138`
- Features Ruff cleanup — completed on 2026-05-03: `src/scalper_ai/features` and selected feature tests now pass targeted Ruff; selected feature pytest passed with `9 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`; full Ruff backlog is now `93`
- RL Ruff cleanup — completed on 2026-05-03: `src/scalper_ai/rl` and selected RL tests now pass targeted Ruff; selected RL pytest passed with `5 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`; full Ruff backlog is now `77`
- Deployment Ruff cleanup — completed on 2026-05-03: `src/scalper_ai/deployment` and selected deployment tests now pass targeted Ruff; selected deployment pytest passed with `58 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`; full Ruff backlog is now `72`
- Execution Ruff cleanup — completed on 2026-05-03: `src/scalper_ai/execution` and selected execution tests now pass targeted Ruff; selected execution pytest passed with `67 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`; full Ruff backlog is now `50`
- Domain and final UTC Ruff cleanup — completed on 2026-05-03: full `.venv/bin/python -m ruff check src tests scripts` now passes with `All checks passed`; selected domain/validation pytest passed with `18 passed`; compileall, `git diff --check`, and full pytest passed with `265 passed`
- Full mypy cleanup — completed on 2026-05-03: `.venv/bin/mypy src` now passes with `Success: no issues found in 87 source files`; full Ruff, compileall, `git diff --check`, targeted pytest (`103 passed`), and full pytest (`265 passed`) passed; `docs/lint-typecheck-baseline.md` records the retired typecheck baseline and the follow-up to promote mypy into the normal gate
- CI/local lint/typecheck gate promotion — completed on 2026-05-03: `make check` now runs Ruff, mypy, compileall, and full pytest; `make ci` adds the safe MT5 preflight; GitHub Actions now runs Ruff and mypy before compile/test/preflight; `make PYTHON=.venv/bin/python ci` passed end-to-end with full pytest `265 passed`
- real-terminal validation and refinement of the new MT5-backed client, reusing the existing reconciliation and connectivity contracts plus the new preflight/auto-discovery layer
- deeper operational hardening for remaining MT5 fault-injection coverage, dependency supervision beyond the current trackers, and longer-duration runtime evidence
- production-readiness cleanup to keep full-repo lint/typecheck green and harden startup ergonomics

## Must-Have Capabilities

- keep paper mode as the default runtime posture unless explicit live-safe conditions are met
- preserve clear boundaries between research, paper, and live execution concerns
- validate the full stack in the actual target runtime, not only the reduced local thread environment
- keep replay/mock fallback available when broker or infrastructure dependencies are unavailable

## Implementation Guidance

- use `docs/post-phase-roadmap.md` as the canonical post-phase backlog
- continue with MT5 terminal validation when the Windows terminal is available; if it is paused, continue with platform/runtime hardening that does not require a broker
- keep deployment and hardening work separate from domain, model, and execution math
- reuse the PHASE 12 runtime, health, and metrics surfaces instead of duplicating startup logic
- favor explicit safety checks, reconciliation, and observability over implicit automation
- use the new paper-safe runtime as the default fallback for environments missing live dependencies

## Validation Goal

- keep repository-wide `pytest` green in Python 3.12.13 and future Python 3.11+ CI/target environments
- add targeted validation around the MT5-backed live adapter, real terminal behavior, non-empty history/deal normalization, reconciliation, and long-running runtime behavior
- keep deployment CLI and paper-safe runtime wiring working while hardening the live path
- keep validation gates and shadow reports as required promotion artifacts for new strategies

## After This Roadmap

- evaluate whether a new formal phase breakdown is needed for live rollout and operations maturity
